[book]
authors = ["assari-harassment"] # あなたのリポジトリ名または名前
language = "en"
title = "ボイスメモアプリのシステムドキュメント" # 適切なタイトルを設定

[output.html]
# 外部のMermaid.jsライブラリを読み込み、DOM読み込み後に実行するスクリプト
additional-head-content = '<script src="https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js"></script><script>document.addEventListener("DOMContentLoaded", function() { mermaid.initialize({ startOnLoad: true }); });</script>'

# 以前の設定で[preprocessor.mermaid]セクションがあった場合は、必ず削除してください